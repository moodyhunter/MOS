# SPDX-License-Identifier: GPL-3.0-or-later

menu "Userspace options"

config MM_LIBALLOC_LOCKS
    bool "Use locks in liballoc"
    default y

config RUST_TARGET
    string "Userspace Rust target"
    default "$ARCH-unknown-mos"

endmenu
