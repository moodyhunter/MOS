name: Build MOS

on:
  push:

jobs:
  build:
    name: "MOS ${{matrix.target}} (${{matrix.build_type}}, Tests: ${{matrix.run_tests}})"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target: [multiboot, multiboot_iso]
        build_type: [Debug, Release]
        run_tests: [ON, OFF]

        exclude:
          - target: multiboot_iso
            run_tests: ON
          - target: multiboot_iso
            build_type: Debug

    steps:
      - uses: actions/checkout@v3

      - name: Install packages
        run: |
          sudo apt update
          sudo apt -y install nasm grub2-common mtools jq gcc-i686-linux-gnu g++-i686-linux-gnu ninja-build qemu-system-x86

      - name: Configure CMake
        run: |
          cmake \
            -DCMAKE_BUILD_TYPE=${{matrix.build_type}} \
            -DMOS_KERNEL_RUN_TESTS=${{matrix.run_tests}} \
            -B ${{github.workspace}}/build \
            -G Ninja

      - name: Get Bootable File
        id: get_bootable
        run: |
          export BOOTABLE=$(jq -r .${{matrix.target}} < ${{github.workspace}}/build/bootables.json)
          echo "::set-output name=bootable::$BOOTABLE"

      - name: Build
        run: |
          cd ${{github.workspace}}/build
          ninja
          ninja ${{matrix.target}}

      - name: Upload Bootable Artifact
        if: matrix.run_tests == 'OFF'
        uses: actions/upload-artifact@v3
        with:
          name: MOS-${{matrix.target}}-${{matrix.build_type}}
          path: ${{steps.get_bootable.outputs.bootable}}

      - name: Run Tests
        if: matrix.run_tests == 'ON'
        id: run_tests
        run: |
          cd ${{github.workspace}}/build
          ${{github.workspace}}/tests/run_tests.sh

      - name: Upload Test Result
        if: matrix.run_tests == 'ON' && failure() && steps.run_tests.outcome == 'failure'
        uses: actions/upload-artifact@v3
        with:
          name: MOS-${{matrix.target}}-${{matrix.build_type}}-failure.ppm
          path: ${{github.workspace}}/build/failure.ppm
